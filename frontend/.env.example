# Copy to .env.local and adjust if needed.
# The Vite dev server proxies /api → http://localhost:8080 by default (see vite.config.ts).
# Override the backend URL here only if your Spring Boot runs on a different port.
# VITE_API_BASE_URL=http://localhost:8080
