# Dev-profile overrides — applied on top of application.properties
spring.jpa.show-sql=true

# Extend JWT access token to 1 hour for convenient Postman testing
app.jwt.access-token-expiration-ms=3600000

# Keep dev uploads separate from any prod uploads folder
app.upload.dir=uploads-dev
